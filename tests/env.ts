import env from '../src/env';

describe('env.ts', () => {

  it('should log if there is no existing config when setting', () => {

  });

  it('should exit if there is no existing config get get, del, and log all', () => {

  });

  it('should log if there is no config for this project', () => {

  });

  it('should log if there is no environment for this project', () => {

  });

  it('should log the project env config', () => {

  });

});
